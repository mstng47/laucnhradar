# Reader Profile — moved

This file is no longer read by the pipeline. The single reader profile it
used to hold has moved to `scripts/profiles/finn.md`, alongside one file
per reader profile the pipeline generates a briefing for — see
`scripts/profiles/index.json` for the full list.

To change what Finn's briefing covers, edit `scripts/profiles/finn.md`
instead of this file.
